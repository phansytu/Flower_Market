import React from "react";
import ReactDOM from "react-dom/client";
import FlowerMarket from "./FlowerMarket";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FlowerMarket />
  </React.StrictMode>
);