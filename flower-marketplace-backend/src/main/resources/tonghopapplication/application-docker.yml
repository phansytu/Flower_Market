spring:
  config:
    activate:
      on-profile: docker

  spring:
  datasource:
    url: jdbc:mysql://host.docker.internal:3306/flower_marketplace?useSSL=false&serverTimezone=UTC
    username: root
    password: 240805

  data:
    redis:
      host: redis
      port: 6379

  elasticsearch:
    uris: http://elasticsearch:9200

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080

logging:
  level:
    root: INFO
